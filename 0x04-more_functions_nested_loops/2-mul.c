#include "main.h"

/**
 * main - multiply
 * Return: 0
 */

int main(void)
{
	int a, b, mul;

	printf("enter 2 numbers to multiply");
	scanf("%d %d", &a, &b);
	printf("Multiplacation = %d, mul");
	return (0);
}
